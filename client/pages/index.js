import Link from 'next/link';

//prop comes from getInitialProps
const LandingPage = ({ currentUser }) => {
  return (
    <div>
      <h1>Landing Page</h1>
    </div>
  )
};

export default LandingPage;